<?php
    // Pengkondisian / Percabangan
    // If-else, If Else-if Else, Ternary, Switch

    // If-else
    // if ( kondisi yang akan di cek)
    // *cek apakah 10 lebih kecil dari 20 jika benar maka akan menampilkan benar 
    // *tapi jika salah akan else menampilkan yang salahnya

    $x = 20;
    if ( $x < 20 ) {
        echo "Benar";
    } else if ( $x = 20 ) {
        echo "Bingo";
    } else {
        echo "Salah";
    }


?>