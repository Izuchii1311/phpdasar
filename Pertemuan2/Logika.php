<?php
    $x = 10;

    var_dump($x < 20 && $x % 2 == 0);
    // Hasilnya True Karena
    // Variabel x 10
    // Jika variabel x kurang dari 20 maka true
    //          &&
    // Jika variabel x modulus% atau habis jika di bagi 2 hasilnya 0 maka true

    // Karena keduanya true maka bool (true)
?>